# 异构多模态动态共识与协同框架 — 项目总结报告

> **撰写日期**：2026-09-01
>
> **阅读指南**：这是一份写给"完全不了解这个项目的人"看的完整报告。无论你是技术背景还是非技术背景，读完这份报告后，你应该能清楚理解：这个项目要解决什么问题、原始设计是什么样的、实际执行中发生了什么、为什么偏离了原计划、最终得到了什么结果、以及接下来要做什么。每一个概念都会用通俗的语言解释，每一个决策都会讲清楚前因后果。

---

## 目录

1. [第一章：这个项目到底在做什么？](#第一章这个项目到底在做什么)
2. [第二章：原始论文设计——一座精美的数学大厦](#第二章原始论文设计一座精美的数学大厦)
3. [第三章：从理论到实验——为什么设计变了？](#第三章从理论到实验为什么设计变了)
4. [第四章：实验全过程的完整故事](#第四章实验全过程的完整故事)
5. [第五章：原始设计被现实逐一推翻的完整记录](#第五章原始设计被现实逐一推翻的完整记录)
6. [第六章：最终实验结果](#第六章最终实验结果)
7. [第七章：经验教训——我们学到了什么](#第七章经验教训我们学到了什么)
8. [第八章：当前状态与后续计划](#第八章当前状态与后续计划)
9. [第九章：论文该怎么写——叙事策略建议](#第九章论文该怎么写叙事策略建议)
10. [附录：文件索引与常用命令](#附录文件索引与常用命令)

---

## 第一章：这个项目到底在做什么？

### 1.1 用一个生活中的例子来理解

想象你是一个**内容审核员**，每天要看几千张网友发的图片（meme，也就是梗图），判断哪些是仇恨言论、哪些不是。

一张典型的 meme 长这样：
- 图片部分：一只表情滑稽的狗
- 文字部分：配文写着 "All immigrants should go back"

你看到这张图，会怎么判断？

- **只看文字**："All immigrants should go back" —— 这明显是仇恨言论，应该标记为违规。
- **只看图片**：一只狗 —— 这跟仇恨言论没有任何关系，看不出任何问题。
- **图文结合看**：文字是仇恨的，但图片是幽默的，整体可能是在讽刺某些人，而不是真的在宣扬仇恨。

**问题来了**：三个不同的判断方式，得出了可能不同的结论。到底听谁的？

### 1.2 这个项目的核心问题

这个项目要回答的核心问题是：

> **如果我有三个不同的 AI 专家（Agent），它们各自用不同的方式看同一张图，得出了不同的结论，我该怎么融合它们的意见，做出最准确的最终判断？**

更进一步：**当它们意见不一致（分歧）的时候，我能不能让它们互相看看对方的推理，重新思考，从而纠正错误？**

### 1.3 三个 AI 专家（Agent）分别是谁

在这个项目中，我们部署了三个 AI 专家，它们各自有不同的"看问题的方式"：

| 专家编号 | 使用的 AI 模型 | 它能看到什么 | 打个比方 |
|---------|-------------|------------|---------|
| **Agent 1** | DeepSeek（一个擅长文本的 AI） | 只看到 meme 上的文字 | 像一个只看文字报告的审核员 |
| **Agent 2** | Gemini（一个擅长图像的 AI） | 直接看图片 + 文字 | 像一个既能看图又能读字的审核员 |
| **Agent 3** | GPT-5.1（一个通用 AI） | 看文字 + 一张"图片的文字描述" | 像一个听别人描述图片是什么的审核员 |

**关键点**：这三个专家的能力差距非常大。Gemini 的正确率高达 80%，而 DeepSeek 和 GPT-5.1 只有 59%。就像一个专家带着两个实习生，**怎么融合才能不被实习生拖后腿？**

### 1.4 一张图看懂整个框架

```
                  ┌──────────────────────────┐
                  │   一张 Meme 图片          │
                  │   （图像 + 文字）          │
                  └────────────┬─────────────┘
                               │
        ┌──────────────────────┼──────────────────────┐
        │                      │                      │
  ┌─────▼──────┐        ┌──────▼─────┐        ┌──────▼─────┐
  │  Agent 1   │        │  Agent 2   │        │  Agent 3   │
  │  DeepSeek  │        │  Gemini    │        │  GPT-5.1   │
  │  只看文字  │        │ 看图+文字  │        │ 文+CLIP描述│
  │  Acc: 59%  │        │  Acc: 80%  │        │  Acc: 59%  │
  └─────┬──────┘        └──────┬─────┘        └──────┬─────┘
        │                      │                      │
        │    每个Agent输出：    │                      │
        │    "仇恨 or 非仇恨"   │                      │
        │    + "我有多自信"     │                      │
        └──────────────────────┼──────────────────────┘
                               │
                  ┌────────────▼────────────┐
                  │   创新 1：不确定性加权    │
                  │   谁更自信就听谁的        │
                  │   Acc: 66.28%            │
                  └────────────┬────────────┘
                               │
                  ┌────────────▼────────────┐
                  │   创新 2：GAT 共识层      │
                  │   识别 Agent 间分歧       │
                  │   47% 的样本有分歧！      │
                  └────────────┬────────────┘
                               │
                  ┌────────────▼────────────┐
                  │   创新 3：因果反思       │
                  │   "如果我知道别人怎么想   │
                  │    我会改变主意吗？"      │
                  │   分歧样本: 40%→68%      │
                  │   全样本: 76.60%          │
                  └─────────────────────────┘
```

### 1.5 这个项目想证明什么

这个项目试图证明三个核心观点：

1. **不确定性加权比固定权重好**：与其猜测哪个 Agent 更厉害，不如让每个 Agent 在每次判断时都报告"我有多自信"，然后根据自信程度动态分配权重。自信的 Agent 说得算，犹豫的 Agent 少发言。

2. **分歧是普遍存在的，需要被重视**：三个 Agent 在将近一半的样本上意见不一致。这些分歧样本是最难判断的，准确率只有 40%，亟待解决。

3. **因果反思可以修正分歧**：当 Agent 意见不一致时，让它们看到其他 Agent 的推理过程，然后重新思考。这个机制可以把分歧样本的准确率从 40% 提升到 68%，接近于最强的单 Agent 水平。

---

## 第二章：原始论文设计——一座精美的数学大厦

### 2.1 设计这个框架时的"野心"

在开始写代码做实验之前，研究者的目标是设计一个**有坚实数学理论支撑**的框架。这个框架不仅仅是"让三个 AI 投票"，而是要从数学上保证：

- **共识一定能达成**（有收敛性证明）
- **即使有恶意 Agent 捣乱，系统也能正常工作**（有拜占庭容错证明）
- **能区分"两个人都很有道理但意见不同"和"有人根本不懂"**（有分歧解构理论）

### 2.2 原始框架的四个层次

原始设计是一个四层架构，每一层都有它的数学原理：

```
第一层：感知层
  ↓    每个 Agent 处理自己的数据，输出一个"信念分布"
  ↓    信念分布 = "我认为是仇恨言论的证据量" + "我认为不是的证据量" + "我的不确定程度"
  ↓
第二层：共识层（内循环）
  ↓    Agent 之间通过图注意力网络（GAT）互相交流
  ↓    不确定性高的 Agent 发言权自动降低
  ↓    数学上保证：经过有限轮交流，一定能达成共识
  ↓
第三层：分歧解构 + 纠偏（外循环）
  ↓    如果共识失败，分析为什么失败
  ↓    是"双方都有理但意见不同"（证据冲突）还是"有人根本不懂"（无知冲突）
  ↓    根据原因采取不同的纠偏策略
  ↓
第四层：决策层
       最终融合所有人的意见
       如果整体不确定性太高 → 拒绝判断（安全优先）
```

### 2.3 每一层具体是怎么设计的

#### 感知层：Dirichlet 分布——用一个数学工具表达"知道多少"

**概念解释**：传统的 AI 分类器输出"90% 是猫，10% 是狗"。但这里的 90% 可能有两个完全不同的含义：
- 情况 A：我见过 1000 只猫，所以我非常确定（真的自信）
- 情况 B：我只见过 3 只猫，但我猜是猫（假自信）

原始设计使用 **Dirichlet 分布** 来区分这两种情况。Dirichlet 分布的核心思想是：**不仅告诉你"我认为是什么"，还告诉你"我基于多少证据得出这个结论"。**

- `α = [10, 1]` 表示：我见过 10 个"是仇恨"的例子和 1 个"不是"的例子，所以我认为是仇恨，而且我比较确定。
- `α = [1.5, 0.5]` 表示：我只见过 1.5 个"是"和 0.5 个"不是"，所以我还是猜"是"，但非常不确定。

从 Dirichlet 参数中可以导出两个关键指标：
- **信念质量 b**：相当于传统概率，但经过了不确定性校正
- **不确定性 u**：值越高代表越无知，范围 0 到 1

#### 共识层：GAT（图注意力网络）——让 Agent 聪明地交流

**概念解释**：三个 Agent 各说各的还不够，需要它们互相交流。GAT 就是一种"有选择的倾听"机制。

- 普通交流：每个人对所有人说的话一视同仁
- GAT 交流：每个人会判断"谁说的话更可信"，然后**选择性倾听**

具体来说，GAT 做三件事：
1. **注意力计算**：判断"我和另一个 Agent 的思维方式相似吗？它有多不确定？"
2. **门控机制**：决定"我要吸收多少对方的信息"（不全盘接受）
3. **状态更新**：融合自己原来的想法和新吸收的信息

**数学保证**：整个过程等价于一个能量函数在下降，就像一个球从山坡上滚下来，最终一定会停在谷底。这保证了**共识一定能收敛**。

#### 分歧解构器：区分两种不同的"意见不合"

**概念解释**：当 Agent 意见不一致时，有两种可能：

- **证据冲突**：两个人都很有道理，但结论相反。就像两个资深医生看了同一张 X 光片，一个说是肺炎，一个说是肺结核。需要辩论。
- **无知冲突**：有人根本不懂。就像一个实习医生看了 X 光片后说"我不知道这是什么"。需要补充信息。

原始设计通过计算 DS 证据理论的**冲突系数 K** 来区分这两种情况：
- K 高 + 双方都自信 → 证据冲突 → 启动证据交换（让最优 Agent 的证据传递给最差 Agent）
- 有人不确定性高 → 无知冲突 → 请求补充信息或拒识

#### 决策层：安全优先的最终判断

最终融合所有人的信念，但如果整体不确定性太高（大家都拿不准），就输出"无法判断"而不是强行判断。这体现了**安全优先**的设计原则。

### 2.4 原始设计中的"理论保证"

原始设计不仅仅是工程设计，还包括了数学证明：

| 理论保证 | 内容 | 数学基础 |
|---------|------|---------|
| 收敛性 | 共识过程一定能在有限步内收敛 | 能量函数梯度下降 = 朗之万扩散 |
| 拜占庭容错 | 即使有 50% 的 Agent 是恶意的，系统仍能正常工作 | 定理 2 的数学证明 |
| 不确定性分解 | 能区分"数据本身有随机性"和"模型缺乏知识" | 主观逻辑的共轭先验 |

### 2.5 原始设计的实验计划

原始设计计划在三个数据集上验证框架：

1. **合成数据**（自己造的）：可以精确控制难度和分歧程度，用来验证基础机制是否 work
2. **Hateful Memes**（Facebook 的仇恨言论数据集）：真正的多模态任务，是实验的主战场
3. **MIMIC-CXR**（医疗 X 光片 + 诊断报告）：高风险场景，验证框架在需要"安全优先"的领域是否有优势

---

## 第三章：从理论到实验——为什么设计变了？

### 3.1 核心矛盾：理论假设 vs 现实约束

原始设计非常精美，但它建立在几个关键假设之上。当真正开始写代码做实验时，这些假设被现实逐一打破：

| 理论假设 | 现实情况 | 后果 |
|---------|---------|------|
| Agent 是"可训练的模型"，可以输出 Dirichlet 分布 | Agent 是第三方 LLM API，只能拿到文本回复和置信度 | Dirichlet 证据学习无法实现 |
| 有足够多的 Agent（至少 5-10 个），GAT 能发挥优势 | 只有 3 个 Agent | GAT 在 3 节点图上几乎退化为加权平均 |
| Agent 的不确定性差异足够大，能触发无知冲突 | 所有 Agent 的自信度都在 0.9 左右 | 无知冲突从不触发 |
| 可以创建"恶意 Agent"来测试拜占庭容错 | LLM Agent 无法被编程为"恶意" | 拜占庭实验无法进行 |
| 纯图像 Agent 在 meme 图上有足够的判断力 | 仇恨言论主要依赖文字，图片几乎不含独立信号 | 纯图像 Agent 准确率只有 42% |

### 3.2 最关键的一个矛盾：LLM API 无法输出 Dirichlet 分布

这是整个项目最根本的偏离。让我详细解释为什么：

**原始设计的设想**：
- 每个 Agent 是一个可训练的神经网络
- 在神经网络的顶层加一个"证据头"（一个小型 MLP）
- 用证据损失函数训练这个证据头，让它输出 Dirichlet 参数 α
- α 的值直接反映了 Agent 累积的证据量

**现实中的情况**：
- 我们使用的是 DeepSeek、Gemini、GPT-5.1 这些**第三方 LLM API**
- 你只能发送一段 prompt 文字，收到一段回复文字
- 你无法在 LLM 内部加一个"证据头"
- 你无法用证据损失函数训练 LLM
- 你只能拿到 LLM 输出的文本和它自己估计的"置信度"

**实际的妥协方案**：
```python
# 这是实际代码中发生的事
u = 1 - confidence    # 不确定性 = 1 - 置信度（完全任意的映射）
S = 2 / u             # 反推总证据强度
alpha = S * probs + 1 # 反推 Dirichlet 参数
```

这段代码的意思是：把 LLM 的置信度直接当成 Dirichlet 的不确定性来用。但这不是真正的 Dirichlet——LLM 的 confidence 是模型自己估计的（可能校准很差），和 Dirichlet 的证据强度 S 没有真实的因果关系。**我们只是把 LLM 的置信度穿了一件 Dirichlet 的外衣。**

### 3.3 第二个关键矛盾：Agent 能力极度不均衡

原始设计假设三个 Agent 的能力大致相当，差异主要来自"看到的模态不同"。但实际实验发现：

| Agent | 准确率 | 差距 |
|-------|--------|------|
| Agent 2（Gemini） | 79.88% | — |
| Agent 1（DeepSeek） | 59.08% | **低 20.8%** |
| Agent 3（GPT-5.1） | 59.16% | **低 20.7%** |

Gemini 比其他两个强了 20 个百分点。这导致一个尴尬的局面：**任何融合方法（包括我们最复杂的框架）都打不过让 Gemini 单独判断。**

这就像你有一个专家和两个实习生，不管你怎么开会讨论，最终结果可能还不如让专家一个人说了算。

### 3.4 第三个关键矛盾：Hateful Memes 数据集本身的特性

原始设计有一个"纯图像 Agent"（Agent 2 只看图片，不看文字）。但实验发现：

- Agent 2 纯图像：准确率 42%（比随机猜测 50% 还低！）
- Agent 2 图像+文字：准确率 79.88%

**原因**：Hateful Memes 数据集中，仇恨言论的判断约 70% 依赖文字。Meme 图本身（一只狗、一个表情包）很少直接表达仇恨。**这不是模型的问题，是数据集本身的特性。**

这导致 Agent 2 从"图像专家"变成了"图像+文字通才"，三个 Agent 的输入不再有本质区别——都是看文字（只是图像理解方式不同）。"异构模态"退化成了"同模态不同视角"。

---

## 第四章：实验全过程的完整故事

> 这一章讲述实验的完整过程，从 2026 年 7 月 17 日到 2026 年 8 月 29 日，按时间顺序展开。每个阶段会讲清楚：**做了什么、发生了什么、为什么这样做、学到了什么。**

### 4.1 背景：实验前的准备工作

在开始 LLM 实验之前，项目已经完成了三个基础步骤：

- **Step 1（合成数据）**：用自己造的简单数据验证了三个 MLP 证据网络的基本功能
- **Step 2（GAT 共识层）**：手写了一个图注意力网络，在合成数据上验证了它能收敛
- **Step 3（分歧解构器）**：实现了基于 DS 证据理论的冲突系数计算，在合成数据上验证了它能区分证据冲突和无知冲突

这三个步骤都是在"完美环境"中完成的——数据是造的、Agent 是可控的 MLP、一切按计划进行。但真正的挑战在于：**把这三个组件搬到真实的 LLM Agent 和真实的数据集上，它们还能工作吗？**

### 4.2 阶段 1：GLM-5V-Turbo 作为 Agent 2（2026-07-17）

**目标**：第一次尝试用真正的 LLM 来运行整个框架。

**配置**：
- Agent 1：DeepSeek（只看文字）
- Agent 2：GLM-5V-Turbo（直接看图片）
- Agent 3：GPT-4o-mini（看文字 + CLIP 生成的图片描述）

**发生了什么**：
- Agent 1 和 Agent 3 正常完成了推理
- Agent 2（GLM-5V-Turbo）一开始还能正常返回，但很快就触发了 API 的限流机制（429 Too Many Requests）
- 限流后，Agent 2 返回的全是 fallback 数据（标签=0，置信度=0.5），相当于"我没看到图，瞎猜的"
- 结果：Agent 2 的准确率只有 53.5%，大量数据是无效的

**为什么失败**：GLM-5V-Turbo 的免费额度有限，连续调用 200 次后就被限流了。API 提供商为了保护服务器，会限制单个用户的调用频率。

**学到了什么**：
- **API 稳定性是实验的第一道门槛**。如果 Agent 不能稳定返回有效结果，再好的框架设计也没用。
- **需要 fallback 机制**。当 API 调用失败时，需要有合理的兜底策略。

**决策**：放弃 GLM-5V-Turbo，寻找更稳定的图像 Agent。

---

### 4.3 阶段 2：Gemini-3.5-flash 作为 Agent 2（2026-07-18）

**目标**：用更稳定的 Gemini 替换 GLM-5V-Turbo。

**配置**：
- Agent 1：DeepSeek（只看文字）
- Agent 2：Gemini-3.5-flash（直接看图片，不看文字）
- Agent 3：GPT-4o-mini（看文字 + CLIP 图片描述）

**做了什么**：
- 先用 5 个样本做小规模验证 → 结果：GAT_EvidenceSwap 准确率 100%！看起来非常好
- 扩大规模到 100 个样本 → 结果：准确率 52%

**发生了什么**：
- 5 样本的 100% 是一个**严重的误导**。因为样本太少，模型恰好在对的那 5 个上表现好，这完全不代表真实能力。
- 扩大到 100 样本后，真相浮出水面：
  - Agent 1（DeepSeek）：Acc=53%，F1=35.62%（严重偏向预测"非仇恨"，漏判了大量仇恨言论）
  - Agent 2（Gemini，仅图像）：Acc=54%，F1=58.93%
  - Agent 3（GPT-4o-mini）：Acc=51%，F1=66.67%
  - GAT_EvidenceSwap：Acc=52%

**学到了什么**：
- **小样本结果绝对不可信！** 5 样本 100% 和 100 样本 52% 的差距告诉我们，永远不能用太少的样本做结论。
- **DeepSeek 在仇恨言论检测上严重偏向保守**。它的 F1 只有 35.62%，说明它大量漏判了仇恨言论。
- **分歧率高达 78-82%**。三个 Agent 在绝大多数样本上意见不一致，说明这个任务确实很难，需要多 Agent 协同。

**决策**：需要优化 DeepSeek 的 prompt，让它更敢于判断仇恨言论。

---

### 4.4 阶段 3：Agent 1 Prompt 优化（2026-07-19）

**目标**：通过修改给 DeepSeek 的提示词（prompt），让它更准确地判断仇恨言论。

**做了什么**：
- 重写了 DeepSeek 的 prompt
- 添加了详细的仇恨言论类型定义（种族歧视、性别歧视、宗教仇恨等）
- 添加了判断原则和推理步骤要求
- 增加了对仇恨样本的判断权重

**结果**：
- Agent 1 F1：35.62% → 36.62%（只提升了 1%）
- 其他指标基本不变

**为什么效果这么差**：
- Prompt 优化是"表面功夫"——你可以让模型更仔细地读题，但你无法改变模型本身的知识和能力。
- DeepSeek 模型本身在仇恨言论检测这个任务上就倾向于保守，这不是改几句话能解决的。
- 就像你让一个性格温和的人"更凶一点"——改改说话方式可以，但本质不会变。

**学到了什么**：**Prompt 优化的天花板很低**。当模型本身的能力有限时，改 prompt 只能带来微小的提升。真正需要的是换一个更强的模型。

**决策**：考虑更换 Agent 1 的模型。

---

### 4.5 阶段 4：Claude Sonnet 5 作为 Agent 1（2026-07-20）

**目标**：用更强的 Claude 模型替换 DeepSeek 作为 Agent 1。

**配置**：
- Agent 1：Claude Sonnet 5（只看文字）
- Agent 2：Gemini-3.5-flash（仅图像）
- Agent 3：GPT-4o-mini（文字 + CLIP 描述）

**结果**：
- Agent 1（Claude）：F1=48.78%，比 DeepSeek 的 36.62% 提升了 33%
- Agent 2（Gemini，仅图像）：Acc=42%（进一步下降，比随机猜测还低！）
- DS_Fusion 融合：Acc=51%
- GAT_EvidenceSwap：Acc=52%

**发生了什么**：
- Claude 确实比 DeepSeek 强，F1 大幅提升。但 Claude 的 API 非常不稳定，频繁返回 500 错误。
- Agent 2（仅图像）的准确率持续下降，从 54% 到 42%。**这进一步证实了"纯图像 Agent 在 Hateful Memes 上不可行"的结论。**
- 融合方法（51-52%）仍然弱于单 Agent 的基线水平。

**关键洞察**：Agent 2 仅看图像时准确率 42%，低于随机猜测 50%。这说明**图像中不仅不包含独立的仇恨信号，还可能在误导模型**（比如把一张中性的图误判为仇恨，或者反过来）。

**决策**：
- Claude 太不稳定，不能作为长期方案 → 考虑回到 DeepSeek 或找其他替代
- Agent 2 必须接收文字，纯图像策略已经被证明不可行

---

### 4.6 阶段 5：因果反事实反思的第一次尝试（2026-07-21）

**目标**：测试因果反思机制——让 Agent 看到其他 Agent 的推理后重新判断。

**做什么**：
- 在 100 个样本上找出 60 个分歧样本
- 分层处理：简单分歧（2v1，即两个 Agent 认为 A，一个认为 B）→ 直接多数投票；复杂分歧（1v1v1，三个 Agent 各说各的）→ 触发完整反思
- 让 Agent 看到其他 Agent 的判断和置信度，然后重新评估

**结果**：
- 分歧样本准确率：48.33% → 63.33%（+15%）
- 收敛率：100%（所有分歧都得到了解决）
- 额外 API 调用：300 次，耗时约 2.5 小时

**为什么这个结果很重要**：
这是因果反思机制第一次在真实数据上展现效果。+15% 的提升虽然不算巨大，但证明了"让 Agent 看到别人的推理然后重新思考"这个思路是可行的。**这为后续的因果反思 v2 版本奠定了基础。**

**学到了什么**：
- 分层策略有效：25% 的简单分歧不需要完整反思，节省了成本
- 跨 Agent 证据交换是核心机制：让 Agent 看到其他 Agent 的判断和置信度，提供了新的信息视角
- 成本可控：300 次额外 API 调用和 2.5 小时是可接受的

---

### 4.7 阶段 6-7：在"稳定"和"异构"之间反复权衡（2026-07-22 至 2026-07-26）

**这段时期的核心矛盾**：到底是追求实验的稳定性（用同一个模型），还是保持论文需要的异构性（用不同模型）？

**方案 A（全用 GPT-4o-mini）**：
- 三个 Agent 都用同一个模型 GPT-4o-mini
- 优点：API 稳定、速度快、结果可复现
- 缺点：失去了"异构多模态"的核心卖点——三个 Agent 一模一样，融合还有什么意义？
- **用户否决了这个方案**，因为偏离了论文的核心定位。

**方案 B（GPT-4o-mini + Gemini + Claude）**：
- 保持异构配置
- 但 Agent 2 从"仅看图像"改为"图像+文字"（因为仅图像的效果已被证明太差）
- 这个改动是**被迫的、务实的**——不是理论指导的，而是实验结果驱动的

**关键转折点**：Agent 2 从"纯图像专家"变为"图像+文字通才"。这个改动意味着原始设计中"三个 Agent 处理不同模态"的理念被打破了——现在三个 Agent 都在看文字，区别只是"如何理解图像"。

---

### 4.8 阶段 8：500 样本大规模实验（2026-08-15 至 2026-08-23）

**这是整个项目最重要的一次实验。**

**目标**：在 500 样本上完整运行整个框架，获得有统计意义的结果。

**配置**：
- Agent 1：GPT-5（只看文字）
- Agent 2：Gemini-3.5-flash（图像+文字）
- Agent 3：GPT-5（文字+CLIP 图片描述）
- 训练集：200 样本，验证集：500 样本

**实验过程**：
- 训练集 200 样本 × 3 Agent 推理：约 1 小时
- GAT 共识训练：100 epochs，loss=0.0554
- 验证集 500 样本 × 3 Agent 推理：约 10.9 小时（39085 秒）
- 总 API 调用：3000 次

**遇到的困难**：
1. **GPT-5 API 返回 403 Forbidden**：第一次大规模运行时，验证集推理到一半 GPT-5 额度用完了，所有样本返回 fallback。需要充值后重新运行。
2. **CLIP 图像描述太慢**：单张图像 CLIP 推理需要计算 76 个候选描述的相似度，一张图就要几秒。后来实现了批量推理（BATCH_SIZE=16），速度提升 5-10 倍。
3. **日志无进度显示**：长时间运行时无法判断是否在正常运行。后来添加了进度打印 + `sys.stdout.flush()` 强制刷新。
4. **编码问题**：日志重定向到文件时使用块缓冲，print 内容不实时显示。需要使用 `python -u` 无缓冲模式。

**核心结果**：

| 方法 | Acc | F1 | 说明 |
|------|-----|-----|------|
| Agent 1（GPT-5，文本） | 55.60% | 40.32% | 文本专家 |
| **Agent 2（Gemini，图像+文本）** | **79.60%** | **80.75%** | **最强单 Agent** |
| Agent 3（GPT-5，跨模态） | 55.00% | 25.74% | 跨模态专家 |
| DS_Fusion（等权重） | 67.80% | 66.59% | 基线融合 |
| **GAT_EvidenceSwap** | **70.40%** | **65.58%** | GAT+证据交换 |
| **Uncertainty_Weighted_DS** | **69.20%** | **68.35%** | 不确定性加权 |

**关键发现**：

1. **融合方法弱于最强单 Agent**：最好的融合方法 70.40% < Gemini 单独 79.60%。**这是一个根本性的问题——如果融合还不如让最强的 Agent 单独判断，那融合的意义是什么？**

2. **Agent 能力极度不均衡**：Gemini 79.6% vs GPT-5 55%，差距 24.6%。弱 Agent 在融合时拖累了强 Agent。

3. **分歧样本 100% 是证据冲突**：239 个分歧样本全部是"证据冲突"类型，"无知冲突"出现了 0 次。**这意味着原始设计中的分歧解构器二分逻辑在真实数据上从未触发过第二种情况。**

4. **GPT-5 训练集 49% 但验证集 55%**：训练集准确率无法预测验证集能力。这为后续的"不确定性加权"提供了最强论据。

5. **Agent 1-Agent 3 相关性 0.651**：同源 GPT-5 模型确实有较高的相关性，验证了异构配置的必要性。

---

### 4.9 阶段 9：调优 + 消融实验（2026-08-23）

**这个阶段是论文实验的转折点。** 它通过系统的消融实验，区分了哪些创新点真正有效、哪些是无效的。

**实验一：相关性折扣（Corr_Aware_DS）调优**

原始设计中有一个"相关性折扣"机制：如果两个 Agent 高度相关（比如都用 GPT-5），就对它们的证据进行打折，避免过度自信。

**结果**：测试了 discount_strength 从 0.0 到 0.7 的所有值，**最佳值是 0.0（即不折扣）**。这说明相关性折扣不仅没有帮助，反而有害。

**为什么**：Agent 1 和 Agent 3 虽然都是 GPT-5，但它们的输入不同（一个只看文字，一个看文字+CLIP 描述），所以它们的预测不是完全冗余的。相关性折扣过度惩罚了有贡献的同源 Agent。

**结论**：相关性折扣被证伪。论文中不能宣传这个创新点。

---

**实验二：不确定性加权（Uncertainty_Weighted_DS）调优**

这是论文的核心创新——基于 Agent 的"不确定性 u"来动态分配权重。u 越低 → Agent 越自信 → 权重越高。

**结果**：测试了 sharpness 从 1.0 到 50.0 的所有值，**sharpness=20 是最佳值**。

| sharpness | Acc | 权重分配 [Agent1, Agent2, Agent3] |
|-----------|-----|----------------------------------|
| 1.0 | 67.80% | [0.334, 0.338, 0.328]（几乎等权） |
| 10.0 | 68.80% | [0.275, 0.493, 0.232] |
| **20.0** | **69.20%** | **[0.265, 0.505, 0.229]** |
| 50.0 | 69.20% | [0.239, 0.583, 0.178] |

**关键发现**：
- Gemini 获得了 50.5% 的权重（最高），**恰好符合它实际最强的能力**。不确定性加权自动识别了强 Agent！
- 与基于训练集准确率的权重方法对比：训练集权重法 Acc=66.00%（最差），不确定性加权 Acc=69.20%（+3.2%）。**不确定性加权显著优于基于训练集统计的权重方法。**

**为什么这个发现很重要**：因为 Gemini 在训练集上只有 49.5% 准确率，如果按训练集表现分配权重，Gemini 会被严重低估。但不确定性 u 是每个样本独立计算的，不依赖训练集统计，所以不受这个问题的困扰。

---

**实验三：GAT 消融**

测试了三种 GAT 配置：
- Symbolic GAT（有意义的符号特征作为 embedding）
- Random GAT（随机投影作为 embedding）
- No GAT（无 GAT，直接 DS 融合）

**结果**：

| 方法 | Acc |
|------|-----|
| No GAT（DS 基线） | 67.80% |
| Random GAT + EvidenceSwap | 69.60% |
| Symbolic GAT + EvidenceSwap | 69.00% |

**关键发现**：
- GAT 架构本身有效（+1.2% vs No GAT）
- **但 Symbolic embedding vs Random embedding 效果相当**（69.0% vs 69.6%，random 反而略好）。这说明 GAT 的注意力机制不依赖于 embedding 的语义含义。
- 论文应该聚焦于 GAT 架构和证据交换机制，而不是 embedding 类型。

---

### 4.10 阶段 10：GAT + 不确定性加权组合实验（2026-08-24）

**目标**：把两个有效创新点（GAT + 不确定性加权）组合起来，看看能不能 1+1>2。

**结果**：**不能。**

| 方案 | Acc | 说明 |
|------|-----|------|
| D（UncDS + EvidSwap，无 GAT） | **70.40%** | **最佳** |
| A（GAT + EvidSwap + UncDS） | 69.00% | GAT 组合反而略降 |
| B（GAT + UncDS） | 69.20% | 与 UncDS 单独持平 |

**为什么 GAT 在组合中无额外增益**：
- 不确定性加权已经通过"u 越低权重越高"实现了类似 GAT 注意力的效果
- GAT 的注意力机制在 3 节点图上表达能力有限
- 两个机制的叠加产生了冗余，甚至可能互相干扰

**结论**：不确定性加权 + 证据交换是性能提升的关键组合，GAT 是冗余的。论文中 GAT 的角色从"核心创新"降级为"辅助组件"。

---

### 4.11 阶段 11：因果反事实反思 v2（2026-08-24）

**这是整个项目中最重要的实验，因为因果反思后来成为了论文的最大卖点。**

**背景**：在阶段 5 中，因果反思 v1（只反思少数派）失败了——27 次改变全部为中性，无法翻转多数投票结果。这次设计了 v2 版本：**反思所有 Agent，不只是少数派。**

**为什么 v1 失败而 v2 成功**：

假设一个分歧场景：Agent 1 和 3 认为是仇恨言论，Agent 2 认为不是（正确答案是"不是"）。

- v1 策略：只让 Agent 2（少数派）看到别人的推理 → Agent 2 可能改变观点 → 从 2v1 变成 3v0 → 多数投票结果不变（仍然是"是仇恨"），但正确答案是"不是" → **无效**
- v2 策略：让所有 Agent 都看到别人的推理 → Agent 1 或 Agent 3（多数派）可能改变观点 → 从 2v1 变成 1v2 → 多数投票结果翻转 → **有效**

**v2 结果**（100 样本，分歧样本）：

| 指标 | 反思前（MV） | 反思后 | 变化 |
|------|------------|--------|------|
| 分歧样本 Acc | 30.00% | **74.00%** | **+44.00%** |
| F1 | 31.37% | **82.89%** | **+51.52%** |
| 正确修正（MV 错→反思对） | — | 50 | — |
| 错误改变（MV 对→反思错） | — | 6 | — |
| **净收益** | — | **+44** | — |

**关键发现**：
- 正确修正:错误改变 = 50:6 ≈ 8.3:1。反思机制非常可靠，不会引入大量新错误。
- F1 提升尤其显著（+51.52%），说明反思特别有效于修正仇恨言论的漏判（提高召回率）。
- 全样本外推（假设反思效果在未处理样本上类似）：Acc 从 60% 提升到 68.80%（+8.80%）。

**为什么这个结果如此重要**：它证明了因果反思是一个**独立的、有效的创新点**，而不是不确定性加权或 GAT 的附属品。它解决的是不同的问题——不是"怎么分配权重"，而是"当 Agent 意见不一致时，怎么让它们重新思考并纠正错误"。

---

### 4.12 阶段 12-13：Agent 配置被迫再次变更 + SOTA 对比（2026-08-24 至 2026-08-26）

**这段时期的核心事件**：API 渠道的不可靠性再次迫使配置变更。

**发生了什么**：
1. GPT-4o-mini 渠道故障（503）→ 无法使用
2. GPT-5 渠道耗尽（503）→ 无法使用
3. 只能切换到 GPT-5.1（GPT-5 的升级版）
4. 同时 Agent 1 从 GPT-5 改为 DeepSeek（因为 DeepSeek 更稳定）

**最终稳定下来的配置**：
- Agent 1：DeepSeek-v4-pro（只看文字）
- Agent 2：Gemini-3.5-flash（图像+文字）
- Agent 3：GPT-5.1（文字+CLIP 描述）

**这个配置不是"精心选择的最优配置"，而是"在 API 限制下还能跑的配置"。** 这一点需要诚实地在论文中说明。

**SOTA 对比实验**：
为了验证框架的竞争力，我们实现了三种主流方法来对比：

| 方法 | Acc | 说明 |
|------|-----|------|
| **Static Ensemble** | **77.80%** | 三个 Agent 等权加权平均（最简单的融合方法） |
| Self-Consistency（n=5） | 53.00% | 同一个 Agent 采样 5 次然后多数投票 |
| Single LLM Multi-Role | 58.40% | 一个 LLM 依次扮演三个角色 |

**关键发现**：最简单的 Static Ensemble 反而是最强的（77.80%），而 Self-Consistency 和 Multi-Role 效果很差。这说明在 Hateful Memes 上，**多 Agent 集成比单 Agent 多次采样或角色扮演更有效**。

---

### 4.13 阶段 14：新配置下 500 样本完整重跑（2026-08-25 至 2026-08-26）

**目标**：在新的 Agent 配置（DeepSeek + Gemini + GPT-5.1）下重跑所有实验，获得公平可比的完整结果。

**最终单种子结果**（seed=42）：

| 方法 | Acc | F1 |
|------|-----|-----|
| Agent 1（DeepSeek） | 59.40% | 46.72% |
| **Agent 2（Gemini）** | **79.20%** | **80.30%** |
| Agent 3（GPT-5.1） | 59.60% | 43.26% |
| MajorityVoting | 62.80% | 51.56% |
| Uncertainty_Weighted_DS | 66.20% | 58.68% |
| GAT_EvidenceSwap | 64.80% | 56.00% |

**与旧配置的对比**：

| 指标 | 旧配置（GPT-5+Gemini+GPT-5） | 新配置（DS+Gemini+GPT-5.1） | 变化 |
|------|----------------------------|---------------------------|------|
| BestAgent | 79.60% | 79.20% | -0.4% |
| 最佳融合 | 70.40% | 66.20% | **-4.2%** |
| 融合-BestAgent 差距 | 9.2% | 13.0% | **差距更大** |

**这说明了一个残酷的事实**：换配置并没有解决核心问题，反而让融合效果更差了。Agent 能力不均衡的问题依然存在，且新配置下 Agent 1 和 Agent 3 的准确率（59%）仍然远低于 Agent 2（79%）。

---

### 4.14 阶段 15：新配置下的因果反事实反思（2026-08-26）

**这是因果反思在新配置下的大规模验证。**

**结果**（seed=42，249 个分歧样本）：

| 指标 | 反思前（MV） | 反思后 | 变化 |
|------|------------|--------|------|
| 分歧样本 Acc | 40.16% | **67.87%** | **+27.71%** |
| F1 | 38.17% | **76.19%** | **+38.02%** |
| 正确修正 | — | 94 | — |
| 错误改变 | — | 25 | — |
| **净收益** | — | **+69** | — |

**全样本外推**：Causal Reflection Acc = 76.60%，与 Static Ensemble（77.80%）的差距仅 1.2%。

**这个结果的意义**：
- 94 个样本被正确修正，25 个被错误改变，净收益 69 个。正确/错误比 = 3.76:1。
- 76.60% vs 77.80%（Static Ensemble），差距仅 1.2%。**因果反思几乎追平了最简单的等权加权平均！**
- 但因果反思比 Static Ensemble 多了可解释性——你知道每个分歧样本是怎么被修正的，有完整的因果推理链。

---

### 4.15 阶段 16：5 种子多种子实验（2026-08-26）

**目标**：用 5 个不同的随机种子（42, 123, 456, 789, 1024）重复实验，验证结果的稳定性。

**为什么需要多种子**：不同的随机种子意味着从数据集中随机抽取不同的训练/验证集划分。如果 5 次结果都很接近（标准差小），说明方法的提升是**系统性的，不是偶然的**。

**最终 5 种子结果**：

| 方法 | Acc（均值 ± 标准差） |
|------|---------------------|
| Agent 1（DeepSeek） | 59.08% ± 0.56% |
| Agent 2（Gemini） | **79.88% ± 0.44%** |
| Agent 3（GPT-5.1） | 59.16% ± 0.50% |
| MajorityVoting | 63.04% ± 0.53% |
| DS_Fusion | 64.20% ± 0.81% |
| **Uncertainty_Weighted_DS** | **66.28% ± 0.52%** |
| GAT_EvidenceSwap | 64.40% ± 0.65% |
| BestAgent | 79.88% ± 0.44% |

**关键发现**：
- 所有方法的标准差都 ≤ 0.87%，Uncertainty_Weighted_DS 仅 0.52%。**结果极其稳定。**
- Uncertainty_Weighted_DS 在所有 5 个种子上都优于其他融合方法。**提升是系统性的，非偶然。**
- 分歧率稳定在 47.4% ± 0.7%。约一半的样本存在 Agent 意见不一致。

---

## 第五章：原始设计被现实逐一推翻的完整记录

### 5.1 断裂点 1：Dirichlet 证据学习 → 退化成了 confidence 映射

**原始设计**：Agent 输出真正的 Dirichlet 分布 α，由证据损失函数训练得到。α 的值直接反映了 Agent 累积的证据量。

**实际代码**：
```python
u = 1 - confidence    # 把 LLM 的置信度直接映射为不确定性
S = 2 / u             # 反推总证据强度
alpha = S * probs + 1 # 反推 Dirichlet 参数
```

**为什么做不到**：原始设计假设 Agent 是"可训练的神经网络"，可以在上面加一个证据头。但实际使用的是第三方 LLM API，你只能发送 prompt 并接收文本回复。你无法在 LLM 内部加任何训练层。

**后果**：论文的整个数学基础（Dirichlet 共轭先验、主观逻辑、信念质量 b、不确定性 u、证据强度 S）都被架空。实验中的"Dirichlet"是用 `u = 1 - confidence` 这个完全任意的映射伪造的。

**论文中如何处理**：诚实承认这个限制。"当前 LLM API 限制下，我们使用 confidence 作为不确定性的代理。未来在可微调的开源模型上将实现真正的证据学习。"

---

### 5.2 断裂点 2：三节点 GAT → 几乎无效

**原始设计**：GAT 共识层是核心创新，有不确定性感知的注意力、门控机制、能量函数、收敛性保证。

**实际结果**：GAT_EvidenceSwap 仅 +0.20% vs DS_Fusion（5 种子均值）。在组合实验中，GAT 无额外增益。

**为什么做不到**：GAT 的能量函数收敛性证明假设了节点数足够多、图结构足够复杂。但实验只有 3 个节点，GAT 在 3 节点完全图上退化为简单的加权平均。**不是 GAT 不好，是图太小了，GAT 的数学优势没有发挥空间。**

**后果**：GAT 从"核心创新 #2"降级为"辅助组件"。论文架构从 4 层降为 3 层。

**论文中如何处理**："GAT 在三节点场景下效果有限，但其架构设计为未来大规模 Agent 网络（5-10 个 Agent）提供了可扩展性。当 Agent 数量增加时，GAT 的注意力机制将发挥更大作用。"

---

### 5.3 断裂点 3：分歧解构器 → 100% 证据冲突，0% 无知冲突

**原始设计**：区分两种分歧——证据冲突（双方都有理但意见不同）和无知冲突（有人根本不懂），触发不同的纠偏策略。

**实际结果**：5 个种子，无知冲突出现了 0 次。所有分歧都是"证据冲突"。

**为什么做不到**：原始设计假设 Agent 的不确定性 u 有足够的方差。但实际 LLM Agent 的 confidence 分布非常集中，所有 Agent 的 u 都在 0.08-0.12 之间。无知冲突的阈值（如 u > 0.5）永远达不到。

**后果**：分歧解构器从"核心创新组件"变成一个从未被触发过的分支。

**论文中如何处理**："在当前 LLM 配置下，Agent 间不确定性差异不足以触发无知冲突。这一现象本身说明 LLM 的置信度校准较好，但未来在异构更强的 Agent（如加入一个明显弱于其他的 Agent）上将验证二分逻辑。"

---

### 5.4 断裂点 4：拜占庭容错 → 从未实验

**原始设计**：定理 2 证明能容忍 50% 恶意节点。

**实际**：从未在任何实验中验证。

**为什么做不到**：你无法创建可控制的"恶意"LLM Agent。注入随机错误预测可以模拟"随机故障"，但 LLM 的"恶意"应该是"故意给出错误推理"，这两者不是一回事。而且，创建恶意 Agent 需要你故意让 LLM 输出错误答案，这在方法论上也很奇怪——你怎么知道什么算"恶意"？

**后果**：论文的核心理论贡献（拜占庭容错证明）没有任何实验支撑。

**论文中如何处理**：作为 future work 讨论。"理论证明后，将在未来工作中构建可控恶意 Agent 环境（如注入已知偏差的 Agent）进行验证。"

---

### 5.5 断裂点 5：纯图像 Agent → 42% 准确率

**原始设计**：Agent 2 是图像专家，只接收图像输入。

**实际结果**：仅看图像时，Agent 2 准确率从 54% 降到 42%，低于随机猜测 50%。

**为什么做不到**：Hateful Memes 中仇恨言论的判断约 70% 依赖文字。一个 meme 的图像部分（一只狗、一个表情包）很少直接表达仇恨。**这不是模型的问题，是数据集本身的特性。**

**后果**：Agent 2 被改为"图像+文字"，"异构模态"退化成了"同模态不同视角"。

**论文中如何处理**：这个失败本身是一个有价值的发现。"我们的实验表明，在 Hateful Memes 任务中，纯图像策略不可行（准确率 42%，低于随机基线）。这证明了文本信息在仇恨言论检测中的核心地位，也说明了为什么需要多模态协同。"

---

### 5.6 断裂点 6：训练集准确率权重 → 最差融合方法

**原始设计**：基于 Agent 在训练集上的准确率分配权重，能力强的 Agent 权重高。

**实际结果**：Acc=66.00%，是所有融合方法中最差的。

**为什么做不到**：训练集只有 200 样本，Gemini 在训练集上过拟合了（倾向于预测非仇恨，导致 Acc 只有 49.5%），但验证集上 Acc=79.6%。按训练集准确率分配权重会严重低估 Gemini。

**后果**：这个失败反而证明了不确定性加权的必要性——u 是 per-sample 的，不受训练集过拟合的影响。

**论文中如何处理**：这是一个有力的对比论点。"基于训练集准确率的权重方法（Acc=66.00%）不仅没有提升，反而比等权重 DS（67.80%）更差。原因是训练集准确率（200 样本）无法预测验证集能力，而我们的不确定性加权基于每个样本的实时置信度，不受此问题困扰。"

---

### 5.7 断裂点 7：多数据集验证 → 只有 Hateful Memes

**原始设计**：合成数据 + Hateful Memes + MIMIC-CXR。

**实际**：只完成了 Hateful Memes。

**为什么做不到**：MIMIC-CXR 是医疗数据，获取需要签署协议。合成数据实验只在 MLP 上验证过，没有扩展到 LLM。MM-IMDb 尝试失败（Agent 能力极度不均衡）。

**后果**：缺少跨数据集泛化验证，是论文的最大短板。

**论文中如何处理**：作为 limitation 讨论，并尽快补充第二数据集实验。

---

### 5.8 断裂点 8：拒识机制 → 从未触发

**原始设计**：全局不确定性高时输出"无法判断"。

**实际**：从未在任何实验中触发。

**为什么做不到**：伪造的 Dirichlet 无法产生足够高的全局不确定性。因为 `u = 1 - confidence`，而 LLM 的 confidence 通常较高（0.85-0.95），所以 u 被限制在 0.05-0.15 之间，拒识阈值（如 u > 0.5）永远达不到。

**后果**：决策层的安全机制形同虚设。

---

## 第六章：最终实验结果

### 6.1 最终 Agent 配置

| Agent | 模型 | 输入 | 5 种子 Acc |
|-------|------|------|-----------|
| Agent 1 | DeepSeek-v4-pro | 仅文本 | 59.08% ± 0.56% |
| Agent 2 | Gemini-3.5-flash | 图像+文本 | **79.88% ± 0.44%** |
| Agent 3 | GPT-5.1 | 文本+CLIP 描述 | 59.16% ± 0.50% |

### 6.2 完整结果表

| 类别 | 方法 | Acc（5 种子均值） | 说明 |
|------|------|------------------|------|
| 单 Agent | Agent 2（Gemini） | **79.88% ± 0.44%** | Oracle 上界 |
| 单 Agent | Agent 1（DeepSeek） | 59.08% ± 0.56% | 文本专家 |
| 单 Agent | Agent 3（GPT-5.1） | 59.16% ± 0.50% | 跨模态专家 |
| 基线 | MajorityVoting | 63.04% ± 0.53% | 等权投票 |
| 基线 | WeightedAvg | 64.47% ± 0.87% | 等权平均 |
| 基线 | DS_Fusion | 64.20% ± 0.81% | 等权重 DS |
| 创新 | **Uncertainty_Weighted_DS** | **66.28% ± 0.52%** | 不确定性加权 |
| 创新 | **GAT_EvidenceSwap** | **64.40% ± 0.65%** | GAT+证据交换 |
| 创新 | **Causal Reflection** | **76.60%（单种子）** | 因果反思 |

### 6.3 SOTA 对比

| 方法 | Acc | F1 |
|------|-----|-----|
| **Static Ensemble** | **77.80%** | **77.30%** |
| **Causal Reflection（本框架）** | **76.60%** | **76.19%** |
| Self-Consistency（n=5） | 53.00% | 52.72% |
| Single LLM Multi-Role | 58.40% | 40.91% |

### 6.4 分歧样本上的因果反思效果

| 指标 | 数值 |
|------|------|
| 分歧率 | 47.4% ± 0.7% |
| 分歧样本 MV 基线 | 39.40% ± 2.34% |
| 分歧样本 Causal Reflection | **67.87%**（seed=42） |
| 正确修正 | 94 个 |
| 错误改变 | 25 个 |
| 净收益 | +69 个 |
| 正确/错误比 | 3.76:1 |

### 6.5 三个创新点的最终评价

| 创新点 | 性能 | 有效？ | 论文角色 |
|--------|------|--------|---------|
| Uncertainty_Weighted_DS | 66.28% ± 0.52%（+2.08% vs DS） | ✅ 有效 | 核心创新 |
| GAT_EvidenceSwap | 64.40% ± 0.65%（+0.20% vs DS） | ⚠️ 微弱 | 辅助组件 |
| Causal Reflection | 76.60%（单种子，+27.71% 分歧） | ✅ 强烈 | 最大卖点 |

---

## 第七章：经验教训——我们学到了什么

### 7.1 关于 API 的使用

1. **永远不要依赖单一 API 渠道**。魔芋平台的 GPT-5 和 GPT-4o-mini 先后耗尽，实验被中断了两次。实验设计应该预留 API 备用方案。

2. **换 key 不等于换渠道**。新旧 key 同属一个分组，当分组渠道耗尽时，换 key 完全无效。

3. **推理模型（如 GPT-5）需要特殊适配**。GPT-5 在 `max_tokens < 2000` 时返回空 content，需要显式设置 `reasoning_effort='minimal'`。

4. **API 速度是实验瓶颈**。Gemini 平均 248 秒/样本，500 样本需要 10 小时以上。在实验设计时就要考虑这个时间成本。

### 7.2 关于实验设计

1. **小样本结果绝对不可信**。5 样本 100% 准确率，扩展到 100 样本后只有 52%。这是整篇报告中最需要记住的教训。**至少 500 样本，最好 1000+。**

2. **多种子是必须的，不是可选的**。5 种子标准差仅 0.52%，证明了结果的稳定性。单种子结果可能是随机波动，审稿人会要求多种子统计。

3. **缓存机制的设计需要前瞻性**。必须按 seed 隔离缓存，修改样本量后必须删除旧缓存。这些看似"小事"的问题，在实际实验中浪费了大量时间。

4. **消融实验的价值极大**。通过消融，我们避免了在论文中宣传无效的创新点（如 Symbolic GAT embedding 和 Corr_Aware_DS）。如果没有消融，论文可能会因为这些无效创新点被审稿人质疑。

### 7.3 关于代码工程

1. **Windows 编码是持续不断的坑**。PowerShell 的 `Set-Content` 默认 UTF-16，会破坏 Python 文件的 UTF-8 编码。解决方案：用 Python 脚本写文件，不要用 PowerShell 的文本操作。

2. **长任务必须有进度显示**。CLIP 为 700 张图像生成描述，没有任何进度打印，无法判断是否在运行。必须添加进度打印 + `sys.stdout.flush()` 强制刷新。

3. **CLIP 批量推理是必须的**。单张图像 CLIP 推理需要计算 76 个候选描述的相似度，太慢。批量推理（BATCH_SIZE=16）可提速 5-10 倍。

### 7.4 关于论文写作

1. **不要追求所有指标都超过 SOTA**。如果你的框架在某些方面不如最简单的方法，那就诚实承认，然后找到框架的独特价值。因果反思 76.60% vs Static Ensemble 77.80% 差 1.2%，但因果反思有可解释性、有因果推理链、能处理分歧。**这些是 Static Ensemble 没有的。**

2. **理论设计必须考虑实际约束**。原始设计中的 Dirichlet 证据学习、GAT 收敛性、拜占庭容错，都建立在"Agent 是可训练的模型"这个假设上。当使用 LLM API 时，这个假设就不成立了。**未来的论文设计应该从实际约束出发，而不是从理想化的数学框架出发。**

3. **Agent 能力不均衡是双刃剑**。它既是挑战（融合被拖累），也是卖点（不确定性加权解决了这个问题）。**论文的叙事应该围绕这个矛盾展开。**

4. **因果反思是意外之喜**。它从原始设计中"最后的兜底策略"变成了实际实验中"唯一的亮点"。**在实验中保持开放心态，最有效的创新点可能不是你最初设计的那个。**

### 7.5 关于探索过程中的方法论

1. **失败的实验比成功的实验更有价值**。Corr_Aware_DS 被证明有害、Symbolic GAT 不比 Random 更好、因果反思 v1 完全无效——这些失败告诉我们什么是死路，从而让我们更聚焦于真正有效的方向。

2. **实验结果的"退化"不一定是坏事**。原始设计从 4 层退化为 3 层，GAT 从核心创新降级为辅助组件——这看起来是"退步"，但实际上是**对真相的逼近**。一篇诚实的论文比一篇假装一切都按计划进行的论文更有学术价值。

3. **实验日志是救命稻草**。15 个阶段的完整记录，让后来者（包括你自己）能理解每一个决策的前因后果。如果没有这份日志，很多决策的动机就会丢失。

---

## 第八章：当前状态与后续计划

### 8.1 已完成（✅）

- Hateful Memes 完整实验（单 Agent、融合、消融、SOTA 对比）
- 5 种子多种子统计验证
- 因果反思大规模实验（seed=42，249 个分歧样本）
- GAT 消融实验
- 不确定性加权调优
- 论文实验部分草稿
- 7 张论文图表
- 测试集评估（1000 样本推理 + 因果反思）

### 8.2 未完成（❌）

| 任务 | 优先级 | 预计工作量 |
|------|--------|-----------|
| 因果反思多种子（3 个种子） | P0 | 2-3 天 |
| 测试集提交 EvalAI 获取官方分数 | P0 | 1-2 天 |
| 第二数据集实验 | P0 | 2-3 周 |
| 错误分析（因果反思失败案例） | P1 | 1 天 |
| 成本分析（API 调用次数、token 消耗） | P1 | 0.5 天 |
| 论文主体撰写（引言、方法、相关工作） | P0 | 3-4 周 |

### 8.3 后续计划（分阶段）

#### 第一阶段：补关键漏洞（1-2 周）

1. **因果反思多种子**：修改 `evaluate_step5_causal_reflection.py` 支持 `--seed` 参数，在 3 个种子上运行因果反思，得到均值 ± 标准差。
2. **测试集提交 EvalAI**：生成预测文件，提交到官方评测平台，获取独立测试集上的分数。
3. **错误分析**：分析 25 个因果反思"错误改变"的案例，分类错误模式，找出反思机制的局限性。

#### 第二阶段：第二数据集（2-3 周）

1. **数据集选型**：从 HuggingFace/Kaggle 上选择一个多模态二分类数据集。推荐候选：SNLI-VE（视觉蕴含，可简化为二分类）、CrisisMMD（危机事件分类）、Food101（食物分类，可简化为二分类）。
2. **数据预处理**：适配现有 pipeline 的数据格式。
3. **LLM 推理**：3 Agent × 500-1000 样本。
4. **全管线实验**：融合 + 因果反思。
5. **结果分析**：与 Hateful Memes 结果对比，分析跨数据集泛化能力。

#### 第三阶段：增强说服力（2-3 周）

1. 增强 SOTA 对比（添加 LLM Debate 等方法）
2. 深度错误分析（分歧样本的类型学分析）
3. Agent 数量消融（2 Agent vs 3 Agent 对比）

#### 第四阶段：论文撰写（3-4 周）

1. 引言（Introduction）
2. 相关工作（Related Work）
3. 方法论（Methodology）
4. 实验部分修订
5. 讨论与结论（Discussion & Conclusion）

---

## 第九章：论文该怎么写——叙事策略建议

### 9.1 核心叙事线

论文不应该以"我们的框架比所有方法都好"为叙事主线（因为事实并非如此），而应该以"**在不完美的 Agent 条件下，如何通过不确定性感知和因果反思实现有效的多 Agent 协同**"为主线。

叙事逻辑链：

```
1. 问题：多 Agent 协同面临 Agent 能力不均衡的挑战
   → Gemini 79.88% vs DeepSeek 59.08%（差距 20%）

2. 第一层解决：不确定性加权 DS 融合
   → 不依赖训练集统计，per-sample 动态权重
   → +2.08% vs 等权重 DS，标准差仅 0.52%

3. 第二层发现：47.4% 样本存在分歧
   → 分歧样本准确率仅 40%，是性能瓶颈

4. 第三层解决：因果反事实反思
   → 让所有 Agent 看到他人的推理后重新评估
   → 分歧样本 40% → 68%（+27.71%）
   → 全样本 76.60%，接近 SOTA（77.80%，差 1.2%）

5. 结论：性能接近 SOTA + 可解释因果推理链 + 系统分歧处理
```

### 9.2 核心卖点排序

1. **Causal Reflection**（最大卖点）：分歧样本 +27.71%，正确/错误比 3.76:1
2. **Uncertainty_Weighted_DS**（核心创新）：解决训练集准确率 ≠ 验证集能力，5 种子稳定 +2.08%
3. **GAT_EvidenceSwap**（辅助角色）：为因果反思提供共识状态和分歧识别

### 9.3 如何诚实处理偏离

| 偏离 | 论文中的处理方式 |
|------|---------------|
| Dirichlet 退化 | 作为 limitation 讨论。"当前 LLM API 限制下，使用 confidence 作为不确定性代理。未来在可微调的开源模型上实现真正的证据学习。" |
| GAT 几乎无效 | 诚实报告。"在 3 Agent 场景下，GAT 的提升有限（+0.20%），但其架构设计为未来大规模 Agent 网络提供了可扩展性基础。" |
| 分歧解构器二分失效 | 诚实报告。"在当前 LLM 配置下，Agent 间不确定性差异不足以触发无知冲突。这一现象本身说明 LLM 的置信度校准较好。" |
| 拜占庭未实验 | 作为 future work。"理论证明后，将在未来工作中构建可控恶意 Agent 环境进行验证。" |
| 纯图像 Agent 失败 | 转化为正面论据。"实验表明纯图像策略在 Hateful Memes 上不可行（Acc=42%），证明了多模态协同的必要性。" |
| 多数据集未完成 | 作为 limitation。"当前仅在 Hateful Memes 上验证，未来将在更多数据集上测试跨领域泛化能力。" |

### 9.4 本框架 vs Static Ensemble 的独特价值

| 维度 | Static Ensemble | 本框架（Causal Reflection） |
|------|----------------|---------------------------|
| Acc | 77.80% | 76.60%（-1.2%） |
| 可解释性 | ❌ 黑盒加权 | ✅ 因果推理链可追溯 |
| 分歧处理 | ❌ 无 | ✅ 分歧样本 +27.71% |
| 不确定性感知 | ❌ 无 | ✅ Agent 级不确定性建模 |
| 纠偏能力 | ❌ 无 | ✅ 跨 Agent 证据交换反思 |
| 安全机制 | ❌ 无 | ✅ 拒识机制（理论设计） |

**核心论点**：我们不以 1.2% 的微小性能差距为代价，换取显著的可解释性提升、分歧处理能力和因果推理链。在某些应用场景（如医疗诊断、法律判断），可解释性比 1.2% 的准确率提升更重要。

---

## 附录：文件索引与常用命令

### 核心代码文件

| 文件 | 功能 |
|------|------|
| `src/step4_hateful_memes/evaluate_with_llm.py` | 主评估脚本，运行所有融合方法 |
| `src/step4_hateful_memes/evaluate_step5_causal_reflection.py` | 因果反思脚本 |
| `src/step4_hateful_memes/evaluate_test_set.py` | 测试集评估脚本 |
| `src/step4_hateful_memes/evaluate_gat_ablation.py` | GAT 消融实验脚本 |
| `src/step4_hateful_memes/generate_charts.py` | 论文图表生成脚本 |
| `src/step2/gat_consensus.py` | GAT 共识引擎实现 |
| `src/step3/disagreement_resolver.py` | 分歧解构器实现 |
| `src/step5/causal_reflection.py` | 因果反事实反思实现 |
| `src/llm_agent.py` | LLM Agent 创建和推理封装 |
| `src/llm_api.py` | LLM API 配置和调用逻辑 |

### 实验结果文件

| 文件 | 说明 |
|------|------|
| `results/hateful_memes/evaluation_llm_deepseek_gemini_gpt5.1_seed{seed}.json` | 5 种子主实验结果 |
| `results/hateful_memes/step5_causal_reflection_v2_seed{seed}.json` | 因果反思结果 |
| `results/hateful_memes/gat_ablation_summary.json` | GAT 消融汇总 |
| `results/hateful_memes/ablation/ablation_results.json` | 消融实验结果 |

### 缓存数据

| 文件 | 说明 |
|------|------|
| `checkpoints/hateful_memes/llm_train_agent{i}_seed{seed}.pt` | 训练集 LLM 推理缓存 |
| `checkpoints/hateful_memes/llm_val_agent{i}_seed{seed}.pt` | 验证集 LLM 推理缓存 |
| `checkpoints/hateful_memes/gat_consensus_llm_seed{seed}.pt` | GAT 模型权重 |

### 配置和文档

| 文件 | 说明 |
|------|------|
| `keys.env` | API 密钥配置 |
| `思维融合.md` | 论文理论设计文档 |
| `概念解释.md` | 核心概念详解 |
| `项目完整说明报告.md` | 早期项目说明 |
| `论文实验部分草稿.md` | 论文实验部分草稿 |
| `EXPERIMENT_LOG.md` | 15 阶段实验日志 |
| `HANDOFF.md` | 项目交接文档 |
| `项目总结报告.md` | 本文档 |

### 常用命令

```bash
# 运行单种子完整实验
python src/step4_hateful_memes/evaluate_with_llm.py --max_train=200 --max_val=500 --provider1=deepseek --provider2=gemini --provider3=gpt5 --seed=42

# 5 种子自动运行
for seed in 42 123 456 789 1024; do python src/step4_hateful_memes/evaluate_with_llm.py --seed=$seed; done

# 因果反思
python src/step4_hateful_memes/evaluate_step5_causal_reflection.py --seed=42

# 生成图表
python src/step4_hateful_memes/generate_charts.py

# 无缓冲模式